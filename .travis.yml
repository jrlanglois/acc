language: cpp

sudo: required
dist: trusty

before_install:
    - export DEBIAN_FRONTEND=noninteractive
    - sudo apt-get update --fix-missing
    - sudo apt-get install -qq
        clang
        libfreetype6-dev
        libx11-dev
        libxinerama-dev
        libxrandr-dev
        libxcursor-dev
        libxcomposite-dev
        mesa-common-dev
        libasound2-dev
        freeglut3-dev
        libcurl4-gnutls-dev+
        libasound2-dev
        libjack-dev
    - sudo apt-get update --fix-missing
    - sudo apt-get update -qq
    - pwd
    - cd ..
    - pwd
    - git clone --depth=1 --branch=master https://github.com/julianstorer/juce.git
    - pwd

compiler:
    - clang

branches:
    only:
        - master

script:
    - cd jrl_acc/Builds/LinuxMakefile
    - make